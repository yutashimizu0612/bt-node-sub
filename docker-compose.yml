version: '3'

volumes:
  db-data:

services:
  web:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - '.:/bt_node_sub'
    environment:
      - 'DATABASE_PASSWORD=postgres'
    tty: true
    stdin_open: true
  #   depends_on:
  #     - db
  #   links:
  #     - db

  # db:
  #   image: mysql:5.7
  #   volumes:
  #     - 'db-data:/var/lib/mysql'
  #   environment:
  #     - 'MYSQL_DATABASE: 'db_name'
  #     - 'MYSQL_ROOT_PASSWORD: 'mysql'
